// Fill => Também pode ter três parametros (o número que substituirá os outros, parametro de início, até onde ele vai substituir)
const numeros = [1, 2, 3];
numeros.fill(0);
console.log(numeros); // Saída: [0, 0, 0]